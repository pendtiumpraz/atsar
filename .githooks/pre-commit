#!/usr/bin/env sh
# Run lint-staged on staged files. Skip if pnpm isn't on PATH (lets non-dev
# environments commit without the dev tooling).
command -v pnpm >/dev/null 2>&1 || exit 0
pnpm exec lint-staged
