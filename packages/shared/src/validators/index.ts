// Shared zod validators — used by both backend (API) and frontend (forms).
// Specific resource schemas live in their own modules (figure.ts, battle.ts, etc.)
// — added in Phase 2/3.

export { z } from 'zod'
